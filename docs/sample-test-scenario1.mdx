---
id: sample_test_scenario
title: Sample Test Scenario
sidebar_label: Sample Test Scenario
slug: /sample-test-scenario
---

import DocCard from '@theme/DocCard';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

# Sample Test Scenario

This section contains multiple test scenarios. Select a scenario from the cards below.

const category = useCurrentSidebarCategory();
const items = (category && category.items) ? category.items : [];
const childItems = items.filter((it) => it && it.docId !== 'sample_test_scenario');

<div className="row">
  {childItems.map((item) => (
    <div key={item.docId || item.href} className="col col--6 margin-bottom--lg">
      <DocCard item={item} />
    </div>
  ))}
</div>



